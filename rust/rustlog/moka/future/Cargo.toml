[package]
name = "moka_future"
version = "0.1.0"
edition = "2024"
description = "异步内存缓存"

[dependencies]
# 引用 Workspace 共享依赖
moka = { workspace = true, features = ["future"] }
tokio = { workspace = true }
