
#### `ringbuf` 无锁单生产者-单消费者(`SPMC`)模式的环形缓冲区

它提供了不同类型的缓冲区，包括适用于单线程环境的 `LocalRb` 和可以在线程间共享的 `SharedRb`，
后者利用动态内存分配（推荐大多数场景使用的是 `HeapRb`）和静态内存分配的选项（如 `StaticRb`）。

> 在处理大量网络数据或实时日志记录时，使用 `ringbuf` 可以有效地缓冲数据，避免频繁的内存分配与复制。
