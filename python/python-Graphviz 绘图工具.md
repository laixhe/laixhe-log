### 安装决策树可视化工具 Graphviz
```
https://www.graphviz.org/download
```

#### 设置 PATH 

> 需要把安装目录的 graphviz/bin 加入环境变量 PATH 里
```
export GRAPHVIZ_DOT=/software/graphviz/bin
export PATH=$PATH:$GRAPHVIZ_DOT
```

#### pip 安装

```
pip install graphviz
```
